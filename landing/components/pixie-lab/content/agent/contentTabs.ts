/** Content workspace accent (the header icon tint). The section navigation itself
 *  now lives in the sidebar submenu — see CONTENT_NAV in lib/pixie-lab/contentRoutes.ts. */
export const CONTENT_ACCENT = '#D4AF37';
